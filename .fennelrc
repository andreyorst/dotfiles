;; -*- mode: fennel -*-

(fn table? [x]
  (= :table (type x)))

(match package.loaded.readline
  (where rl (table? rl))
  (rl.set_options {:histfile  "~/.fennel_history"
                   :keeplines 1000}))
