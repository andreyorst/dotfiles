%sh{
	for file in $(echo $HOME/.config/kak/*.kak); do
		echo source "$file"
	done
}

plug alexherbo2/snippets.kak

hook global WinCreate .* %{
    snippets-enable
}

set-option global snippets %{{
    "->": "→"
    "ttt": "tos(\"$1\"); ton($2); tos(\"${3:\n\r}\");"
}}

