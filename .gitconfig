[alias]
	clean-all = clean -xdf
	st = status
	co = checkout
	ci = commit
	cim = commit -m
	br = branch
	bra = branch -a
        root = rev-parse --show-toplevel
	exec = "!exec "
[pull]
	ff = only
	rebase = false
[include]
	path = .gitconfig.local
